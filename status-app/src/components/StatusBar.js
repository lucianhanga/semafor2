import React from "react";
import "./StatusBar.css"; // Import the CSS file for styling

const StatusBar = () => {
  return (
    <div className="status-bar">
    </div>
  );
};

export default StatusBar;